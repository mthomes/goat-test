/**
 * Astro 7's content-config entry point. The collection itself is defined in
 * `src/content/config.ts`, which is where the schema and its documentation live.
 */
export { collections } from "./content/config";
